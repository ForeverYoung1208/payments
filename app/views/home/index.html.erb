<div ng-controller='HomeController'>
  <nav class="navbar navbar-default navbar-fixed-top">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" ng-init="isCollapsed = true" ng-click="isCollapsed = !isCollapsed">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>

        <%= glyph_link_to('', root_path, class: 'navbar-brand') {'Согласование платежей'} %>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="navbar-collapse" ng-class="{collapse: isCollapsed}">
        <ul class="nav navbar-nav">
          <li ng-class="{active : isActive(1)}"> <%= link_to 'Заявки', requests_path %> </li>
          <li ng-class="{active : isActive(2)}" class="dropdown" >
            <a href="#" data-toggle="dropdown" ng-click="clickMenu(2)"> Справочники </a>
            <ul class="dropdown-menu">
                <li><%= link_to 'Статьи бюджета', b_accounts_path, target: '_blank' %> </li>
                <li><%= link_to 'Проекты', projects_path, target: '_blank' %> </li>
                <li><a href="#">Another action</a></li>
            </ul>
          </li>
          <li ng-class="{active : isActive(3)}"><a ng-click="clickMenu(3)" href="#">Резерв</a></li>
        </ul>


        <%= render 'user_zone' %>
      </div><!-- /.navbar-collapse -->


    </div><!-- /.container-fluid -->
  </nav>

  <section>
    <div class="row">    
      <h5 class='col-sm-offset-2 col-sm-8 text-center page-header'> Полезная инфомрация для стартового экрана</h5>
    </div>

  </section>

</div>
